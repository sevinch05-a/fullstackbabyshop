// Controller Controlling? Functionality of Data based of HTTP methods and REST routing 
//const Note = require('../models/product');


// CRUD ---> find() findByID()  findBYIdAndUpdate() findByIdAndDelete()

//const fetchNotes = async(req,res) => {
  
//};
// -------------------------[READ]
//const fetchNote = async(req,res) => {
   // const noteId = req.params.id
   // const note = await Note.findById(noteId)
   // res.json({note: note})
//};



//----------------[READ for :id]

//const createNote = async  (req,res) => {
    //const title = req.body.title
    //const body = req.body.body
    //const note = await Note.create({
       //title: title,
       //body: body 
    //})
    //res.json({note:note})
//};
// -----------------------------[CREATE]
//const updateNote = async (req,res) => {
   //const noteId = req.params.noteId
   //const {title,body} = req.body 
   //const note = Note.findByIdAndUpdate(noteId, {
    //title: title,
    //body: body
   //})

   //const updateNote = Note.findById(noteId)

   //res.json({note: updateNote})

//}
//----------------------------[UPDATE]
//const deleteNote = async (req,res) => {
    //await console.log("is deleted")
//}
// ---------------------------[DELETE]


//module.exports = {
    //fetchNotes,
    //fetchNote,
    ////createNote,
    //updateNote,
    //deleteNote 
//}
